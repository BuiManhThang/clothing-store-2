export interface CreateFileDTO {
  name: string;
  status: string;
}

export interface UpdateFileDTO {
  name: string;
  status: string;
}

export interface ViewFileDTO {
  name: string;
  status: string;
}
