export interface {{EntityName}}DtoCreate {
{{#each createDtoColumns}}
  {{column_name}}: {{data_type}};
{{/each}}
}

export interface {{EntityName}}DtoUpdate {
{{#each createDtoColumns}}
  {{column_name}}: {{data_type}};
{{/each}}
}

export interface {{EntityName}}DtoView {
{{#each columns}}
  {{column_name}}: {{data_type}};
{{/each}}
  createdAt: Date
  createdBy: string
  modifiedAt?: Date
  modifiedBy?: string
}
